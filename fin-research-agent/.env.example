# Copy to .env (which is gitignored). The default CLI + Ollama path needs NO API key.
MODEL=gemini-2.5-flash
DB_PATH=./data/research.db
MAX_STEPS=8
TRACE_DIR=./traces
OLLAMA_URL=http://localhost:11434
EMBED_MODEL=nomic-embed-text
LLM_TIMEOUT_SEC=120
